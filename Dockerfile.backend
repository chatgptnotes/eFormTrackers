FROM node:22-alpine

WORKDIR /app/backend

COPY backend/package.json backend/package-lock.json ./
RUN npm ci --omit=dev

COPY backend ./
RUN mkdir -p uploads logs

EXPOSE 3001

CMD ["node", "server.js"]
